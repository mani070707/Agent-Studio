# Supabase project (Auth + Postgres + Storage) — create at supabase.com (free tier)
SUPABASE_URL=
SUPABASE_JWKS_URL=
DATABASE_URL=

# Key used to encrypt/decrypt user BYOK secrets (user_secret.encrypted_value) — generate your own
SECRET_ENCRYPTION_KEY=
