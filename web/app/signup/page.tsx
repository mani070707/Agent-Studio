"use client";
import {useState} from "react";import Link from "next/link";import {supabase} from "@/lib/supabaseClient";
export default function SignupPage(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);async function handleSubmit(e:React.FormEvent){e.preventDefault();setError("");setMessage("");setLoading(true);const{error}=await supabase.auth.signUp({email,password});setLoading(false);if(error){setError(error.message);return;}setMessage("Account created. Check your email to confirm it, then log in.");}
 return <main className="auth-shell"><section className="auth-brand-panel"><div className="auth-brand"><span>A</span><strong>Agent Studio</strong></div><div className="auth-brand-copy"><span>One workspace, complete lifecycle</span><h1>Build agents your team can actually trust.</h1><p>Go from an idea to a tested, versioned agent with controlled tools and predictable output.</p><div className="auth-quote">“A clear path from prompt experimentation to an operational agent.”</div></div><small>Your keys remain encrypted and server-side.</small></section><section className="auth-form-panel"><form className="auth-form-modern" onSubmit={handleSubmit}><div className="auth-mobile-brand"><span>A</span> Agent Studio</div><span className="ds-eyebrow">Create workspace access</span><h2>Start building agents</h2><p>Create your account with an email and password.</p>{error&&<p className="form-error alert-box">{error}</p>}{message&&<p className="form-success alert-box success">{message}</p>}<div className="field"><label>Email address</label><input type="email" autoComplete="email" required placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" autoComplete="new-password" required minLength={6} placeholder="At least 6 characters" value={password} onChange={e=>setPassword(e.target.value)}/><small className="field-help">Use at least six characters.</small></div><button className="btn auth-submit" type="submit" disabled={loading}>{loading?"Creating account…":"Create account →"}</button><p className="auth-switch">Already have an account? <Link href="/login">Log in</Link></p></form></section></main>}
