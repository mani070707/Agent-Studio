"use client";
import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabaseClient";
export default function LoginPage(){const router=useRouter();const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function handleSubmit(e:React.FormEvent){e.preventDefault();setError("");setLoading(true);const{error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error){setError(error.message);return;}router.push("/agents");}
 return <main className="auth-shell"><section className="auth-brand-panel"><div className="auth-brand"><span>A</span><strong>Agent Studio</strong></div><div className="auth-brand-copy"><span>Build with confidence</span><h1>Turn repeatable work into reliable AI agents.</h1><p>Design prompts, connect tools, test structured output, and publish dependable workflows from one workspace.</p><div className="auth-features"><div><b>01</b><span><strong>Compose</strong><small>Models, prompts, schemas, and knowledge</small></span></div><div><b>02</b><span><strong>Validate</strong><small>Playground traces and evaluation gates</small></span></div><div><b>03</b><span><strong>Operate</strong><small>Versioned agents with observable runs</small></span></div></div></div><small>Agent Studio · Local workspace</small></section><section className="auth-form-panel"><form className="auth-form-modern" onSubmit={handleSubmit}><div className="auth-mobile-brand"><span>A</span> Agent Studio</div><span className="ds-eyebrow">Welcome back</span><h2>Log in to your workspace</h2><p>Continue building and testing your agents.</p>{error&&<p className="form-error alert-box">{error}</p>}<div className="field"><label>Email address</label><input type="email" autoComplete="email" required placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" autoComplete="current-password" required placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/></div><button className="btn auth-submit" type="submit" disabled={loading}>{loading?"Logging in…":"Log in →"}</button><p className="auth-switch">New to Agent Studio? <Link href="/signup">Create an account</Link></p></form></section></main>}
